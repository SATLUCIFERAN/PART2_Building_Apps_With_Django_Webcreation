
// Selects the first paragraph
const firstParagraph = document.querySelector('p');

// Selects the element with ID 'magicMessage' (same as getElementById)
const magicMessageById = document.querySelector('#magicMessage');

// Selects the first element with class 'container'
const firstContainer = document.querySelector('.container');
