


// if placed in <head> without 'defer'
document.addEventListener("DOMContentLoaded", () => {
    // This code will only run after the entire HTML document has been loaded and parsed
    const magicMessage = document.querySelector('#magicMessage');
    const activateButton = document.querySelector('#activateButton');
    // ... rest of your code that interacts with DOM ...
});




